#import '../fragments/seo.graphql'

query($slug: String!) {
    pageBySlug(slug: $slug) {
        id
        slug
        title
        date
        modified
        content
        seo {
            ...SeoParts
        }
    }
}